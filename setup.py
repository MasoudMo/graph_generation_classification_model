from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Integrating the graph generation process into the classification process using GNNs.',
    author='Masoud Mokhtari',
    license='MIT',
)
